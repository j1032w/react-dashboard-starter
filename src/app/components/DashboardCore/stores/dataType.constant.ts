export const DASHBOARD_STATE = 'dashboardState';

export const enum DashboardCoreActionTypes {
  AddWidget = 'Add widget',
  RemoveWidget = 'Remove widget',
  RestoreAllWidgets = 'Restore widgets',
  RemoveAllWidgets = 'Remove all widgets',
}
